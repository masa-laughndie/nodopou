<% provide(:title, "パスワードを忘れた") %>
<% provide(:og_title, "パスワードを忘れた") %>
<% provide(:og_url, new_password_reset_url) %>

<div class="container content-mspace">
  <div class="title-mspace c">
    <h1>パスワードを忘れた</h1>
  </div>
  <div class="attention">
    <p>仮ログインURLをメールアドレスに送信します。登録済みのメールアドレスを入力してください・</p>
  </div>
  <div class="form">
    <%= form_for(:password_reset, url: password_reset_path) do %>
      <div class="field">
        <%= email_field_tag :email, nil, placeholder: "登録済みのメールアドレス" %>
      </div>
      <div class="button-middle button-accent">
        <%= submit_tag "仮ログインURLを発行", data: { disable_with: "処理中..." } %>
      </div>
    <% end %>
  </div>
</div>