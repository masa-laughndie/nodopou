<div class="button-unavail">
  <%= form_for(current_user.mylists.find_by(list_id: list.id),
               html: { method: :delete },
               remote: true) do |f| %>
    <div><%= hidden_field_tag :list_id, list.id %></div>
    <%= f.button "便乗済み" %>
  <% end %>
</div>