<% provide(:title, @user.name + "(@" + @user.account_id + ")さんのリスト一覧") %>
<% provide(:og_title, @user.name + "(@" + @user.account_id + ")さんのリスト一覧") %>
<% provide(:og_url, mylists_url(@user)) %>

<div class="container content-nspace">
  <div class="title-nspace c">
    <% if current_user?(@user) %>
      <h1>マイリスト一覧</h1>
    <% else %>
      <h1><%= @user.name %>さんのリスト一覧</h1>
    <% end %>
  </div>
  <div class="border"></div>
  <% if @mylists.any? %>
    <% if current_user?(@user) %>
      <%= render partial: 'shared/list_item',
                 locals: { left: '実行状況',
                           center: '内容',
                           right: 'Dayチェック' } %>
    <% else %>
      <%= render partial: 'shared/list_item',
                 locals: { left: '実行状況',
                           center: '内容',
                           right: '便乗？' } %>
    <% end %>
    <ul>
      <%= render partial: @mylists,
                 locals: { user: @user,
                           cuser_list_ids: @cuser_list_ids } %>
    </ul>
  <% else %>
    <%= render partial: 'shared/no_data',
               locals: { icon: 'list',
                         text: 'list-search',
                         user: @user } %>
  <% end %>
</div>